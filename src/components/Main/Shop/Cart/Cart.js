import React, { Component } from 'react';
import { StackScreenAtCart } from '../../../../Router';

export default class Cart extends Component {
    render() {
        return (
            <StackScreenAtCart />
        );
    }
}
