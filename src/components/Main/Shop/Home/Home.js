import React, { Component } from 'react';
import { StackScreenAtHome } from '../../../../Router';

export default class Home extends Component {
    render() {
        //const { types } = this.props;
        return (
            <StackScreenAtHome />
        );
    }
}
