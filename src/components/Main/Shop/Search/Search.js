import React, { Component } from 'react';
import { StackScreenAtSearch } from '../../../../Router';

export default class Cart extends Component {
    render() {
        return (
            <StackScreenAtSearch />
        );
    }
}
